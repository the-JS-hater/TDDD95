#include <iostream>
#include <algorithm>
#include <set>
#include <string>
#include <vector>
#include <stack>


using std::cin;
using std::cout;
using std::ios::sync_with_stdio;
using std::string;
using std::vector;


void setStack(vector<string> v)
{
	stack<set>
	for (string s: v)
	{
		switch(s) 
		{
			case "PUSH": {
				break;
			};
			case "ADD": {
				break;
			};
			case "UNION": {
				break;
			};
			case "DUP": {
				break;
			};
			case "INTERSECT": {
				break;
			};
		}
	}
}


int main()
{
	int t, n;
	string s;
	cin >> t;
	for (unsigned i {0}; i < t; i++)
	{
		cin >> n;
		vector<string> v;
		for (unsigned j {0}; j < n; j++) 
		{
			cin >> s;
			v.push_back(s);
		}

		// setStack()
		// print res, maybe
		//

	}
}
